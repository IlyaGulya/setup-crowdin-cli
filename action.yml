name: 'Setup Crowdin CLI'
description: 'Set up Crowdin CLI in your GitHub Actions workflow'
author: 'Ilya Gulya'
branding:
  icon: 'download'
  color: 'blue'

inputs:
  version:
    description: 'Version of Crowdin CLI to use (e.g. 3.16.0)'
    required: false
    default: 'latest'

runs:
  using: 'node20'
  main: 'dist/index.js' 
